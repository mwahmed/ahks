<div class="container">
	<div class="well">
		<h1> <%= @type.capitalize %> Deliverables </h1>
	
		<%@re_array.each do |r| %>
			
			<p> <%e= r.split("++++") %> <%= highlight(e[0], e[1].split) if !e[1].nil?%></p>
	
		<%end %>
	</div>
</div>

